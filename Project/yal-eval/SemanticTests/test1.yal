module aval6_err
{
c;
d = [5];
e = [6];
function a=sqrt(vsqn, ad) { 
	
	a = 1;
	b = 2;
	if(a < b){ // no error
		
	}

	if(a > c){//error

	}
	
	if(c < a){//error

	}
	
	if(d < e){//error

	}

	if(a < e){//error

	}
	if(e > a){//error

	}

	if(d.size < e.size){//no error
	
	}
	
	if(d.size > e[4]){//no error
	
	}
	if(e == d){//no error

	}
	if(d.size != d){//error

	}

	f=[5];
	g = [6];
	
	if(f == g){//no error
	
	}
	
     	
}

}
